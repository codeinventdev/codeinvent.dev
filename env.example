# Email Configuration for Contact Form
# Copy this file to .env.local and fill in your actual values

# Domain Email SMTP Configuration
SMTP_HOST=smtp.codeinvent.dev
SMTP_PORT=587
SMTP_SECURE=false
EMAIL_USER=dev@codeinvent.dev
EMAIL_PASS=your-domain-email-password

# Instructions:
# 1. Replace smtp.codeinvent.dev with your actual SMTP server
# 2. IMPORTANT - Port and Security Settings:
#    - Port 587 + SMTP_SECURE=false (STARTTLS) ← Most common, recommended
#    - Port 465 + SMTP_SECURE=true (Direct SSL)
#    - Port 25 (Plain) - Usually blocked by hosting providers
#    
#    ⚠️  CRITICAL: Port 587 with SMTP_SECURE=true will cause SSL errors!
#    ⚠️  Always use SMTP_SECURE=false with port 587
#
# 3. Replace dev@codeinvent.dev with your actual domain email
# 4. Use your domain email password for EMAIL_PASS
# 5. Contact your hosting provider for SMTP server details if unsure

# Common hosting provider SMTP settings:
# cPanel/WHM: smtp.yourdomain.com
# Hostinger: smtp.hostinger.com
# GoDaddy: smtpout.secureserver.net
# Namecheap: mail.privateemail.com
# Bluehost: mail.yourdomain.com
